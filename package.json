{
  "name": "kumo-marketplace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test --if-present",
    "altana:wallet-preflight": "node scripts/altana-wallet-preflight.mjs",
    "rebalancer:stateful-canary": "node scripts/rebalancer-stateful-canary.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
