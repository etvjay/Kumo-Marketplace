name: Venus Core Account Evidence

on:
  push:
    branches: [main]
    paths:
      - "packages/reference-agents/src/health/**"
      - "scripts/venus-core-account-evidence.mjs"
      - ".github/workflows/venus-core-account-evidence.yml"
  workflow_dispatch:
    inputs:
      account:
        description: "Public Venus Core account to verify"
        required: false
        default: "0xcD5e600c3335524bb6572a0Ebd639aA5ea4EfF0F"
      discovery_ref:
        description: "Public discovery evidence reference"
        required: false
        default: "bscscan:tx:0x94f3eae78f1f0b2c00bdf5ae05d41982fb77327cf32351a84f7394486a4d0b51"

jobs:
  evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Enable Corepack and pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.8.1 --activate
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      - name: Build workspace
        run: pnpm build
      - name: Verify public Venus Core borrower against finalized BSC state
        env:
          BSC_RPC_URL: https://bsc-dataseed.bnbchain.org
          VENUS_EVIDENCE_ACCOUNT: ${{ inputs.account || '0xcD5e600c3335524bb6572a0Ebd639aA5ea4EfF0F' }}
          VENUS_ACCOUNT_DISCOVERY_REF: ${{ inputs.discovery_ref || 'bscscan:tx:0x94f3eae78f1f0b2c00bdf5ae05d41982fb77327cf32351a84f7394486a4d0b51' }}
          KUMO_EVIDENCE_PATH: evidence/live/venus-core-account-evidence.json
        run: node scripts/venus-core-account-evidence.mjs
      - name: Upload public account evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: venus-core-account-evidence-${{ github.sha }}
          path: evidence/live/venus-core-account-evidence.json
          if-no-files-found: error
          retention-days: 30
